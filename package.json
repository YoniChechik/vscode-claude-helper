{
  "name": "git-diff-viewer",
  "displayName": "Git Diff Viewer",
  "description": "View git diff files between HEAD and origin default branch",
  "version": "0.0.8",
  "publisher": "local",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/git-diff-viewer"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:gitDiffExplorer"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gitDiffContainer",
          "title": "Git Diff Viewer",
          "icon": "$(git-compare)"
        }
      ]
    },
    "views": {
      "gitDiffContainer": [
        {
          "id": "gitDiffExplorer",
          "name": "Git Diff (HEAD vs origin)"
        }
      ]
    },
    "commands": [
      {
        "command": "gitDiffExplorer.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "gitDiffExplorer.openDiff",
        "title": "Open Diff"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "gitDiffExplorer.refresh",
          "when": "view == gitDiffExplorer",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.28.0",
    "typescript": "^4.9.3"
  }
}
