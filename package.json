{
  "name": "git-diff-viewer",
  "displayName": "Git Diff",
  "description": "View git diff files",
  "version": "0.6.0",
  "publisher": "local",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/git-diff-viewer"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gitDiffContainer",
          "title": "Git Diff",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "gitDiffContainer": [
        {
          "id": "gitDiffExplorer",
          "name": "Changes",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "gitDiffExplorer.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "gitDiffExplorer.openDiff",
        "title": "Open Diff"
      },
      {
        "command": "gitDiffExplorer.changeComparisonTarget",
        "title": "Change Comparison Target",
        "icon": "$(git-compare)"
      },
      {
        "command": "gitDiffExplorer.toggleViewMode",
        "title": "Toggle View as Tree / List",
        "icon": "$(list-tree)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "gitDiffExplorer.refresh",
          "when": "view == gitDiffExplorer",
          "group": "navigation"
        },
        {
          "command": "gitDiffExplorer.toggleViewMode",
          "when": "view == gitDiffExplorer",
          "group": "navigation"
        },
        {
          "command": "gitDiffExplorer.changeComparisonTarget",
          "when": "view == gitDiffExplorer",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.28.0",
    "typescript": "^4.9.3"
  }
}
